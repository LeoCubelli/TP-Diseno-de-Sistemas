{{#partial "navItems"}}
    <ul class="navbar-nav ml-auto">
        <li class="nav-item">
            <a class="nav-link page-scroll" href="/form-sin-chapita">Encontré Una Mascota</a>
        </li>
    </ul>
    {{>signInButton.hbs}}
{{/partial}}

{{#partial "content"}}
    <header class="ex-header">
        <div class="container">
            <div class="row">
                <div class="col-xl-10 offset-xl-1">
                    <h1 class="text-center">Iniciar Sesión</h1>
                </div>
            </div>
        </div>
    </header>

    <div class="ex-form-1 pt-4 pb-4">
        <div class="container">
            <div class="row">
                <div class="col-xl-6 offset-xl-3">
                    <div class="text-box mt-5 mb-5">
                        <p class="mb-4">¿No tenés una cuenta? <a class="blue" href="/duenio/signup">Registrate</a></p>
                        <form id="logInForm" action="/duenio/login" method="POST">
                            <div class="form-group">
                                <input type="text" class="form-control-input" id="text" name="usuario" value="{{ usuario }}" required>
                                <label class="label-control" for="email">Usuario</label>
                            </div>
                            <div class="form-group">
                                <input type="password" class="form-control-input" id="password" name="password" value="{{ password }}" required>
                                <label class="label-control" for="password">Contraseña</label>
                            </div>
                            <div class="form-group">
                                <button type="submit" class="form-control-submit-button">Iniciar Sesión</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{{/partial}}

{{>layout.hbs}}